# Approved issue LLM scoring proof

VOUCHA uses an LLM semantic score to compare an approved issue with the pull
request title, body, and changed files. This validation policy requires a score
of at least 0.7 before the approved-issue exemption is granted.
